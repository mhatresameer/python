# if we want to specify the data-types and change it accordingly, we use casting for variables.
username = "Sameer"
type(username)

# we are assigning a variable the number which is a whole number and an integer.
age = 30
print(age)
# knowing the type before we type-cast.
type(age)

# here we just type-cast the existing integer to float by printing it and assigning it directly.
print(float(age))
